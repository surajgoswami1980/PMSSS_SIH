<!-- Navbar Section -->
 
<style>
    /* Styling for the All Categories Dropdown */
    .category-section1 {
      width: 100%;
      
    }
    
  
    .collapse {
      display: none;
    }
  
    .collapse.show {
      display: block;
    }
  
    /* Optional: Add some padding and background to the dropdown content */
    #allCategoriesDropdown {
      background-color: #f8f9fa;
      z-index: 1000; 
      position: relative;
      z-index: 1000; 
      padding: 20px;
      
    }
    #allCategoriesDropdown.show {
  display: block;
}
  
    /* Adjustments for the category list */
    #allCategoriesDropdown ul {
      list-style-type: none;
      padding-left: 0;
    }
  
    #allCategoriesDropdown ul li {
      margin-bottom: 5px;
    }
    .profile-img {
  width: 50px;
  height: 50px;
  object-fit: cover;
}

/* Default Profile Icon */
.profile-icon {
  font-size: 50px;
  color: #6c757d;
}
.profile-img {
    border-radius: 50%;
    margin-bottom: .5rem;
  }
  @media (max-width: 768px) {
  .navbar-nav {
    text-align: center;
  }
  .custom-button1,.custom-login-btn1 ,.nav-link{
    background-color: none !important;
    border: none !important;
    color: black !important;
    margin-top: 5px !important;
    margin-bottom: 5px !important;
  }
  
  .navbar-nav .nav-link {
    margin: 0.5rem 0;
    text-align: center !important;
  }
}

  </style>
<nav class="sticky-top navbar navbar-expand-md  bg-white border-bottom custom-navbar1">
    <div class="container-fluid">
      <a class="navbar-brand text-light " href="/listings">
        <div class="icon-image mx-3" style="width: 120px; height: 50px;">
          <img src="/15175645.png" style="width: 100%; height: 100%; ">
        </div>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup1" aria-controls="navbarNavAltMarkup1" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup1">
        <div class="navbar-nav me-auto">
          <!-- <a class="nav-link text-dark ms-3" aria-current="page" href="/listings">Home</a> -->
          <a class="nav-link fw-bold text-dark mx-3" href="/listings">All Scholarship</a>
          <% if(!currUser) {%>
            <a class="nav-link custom-button1 ms-3" href="/apply">Apply Now</a>
            <% }else{%>
                 <% if (currUser.role === "student" ) { %>
                      <a class="nav-link custom-button1 ms-3" href="/apply">Apply Now</a>
                <% } else if (currUser.role === "admin") { %>
                    <a class="nav-link custom-button1 ms-3" href="/listings/new">Add New Scholarship</a>
                <% } else if (currUser.role === "college") { %>
                   
                <% } else if (currUser.role === "bank") { %>
                   
                <% } %>
        <% } %>

        </div>
        <div class="navbar-nav ms-auto">
          <% if (!currUser) { %>
            <a class="nav-link custom-login-btn1 mx-3" aria-current="page" href="/login">Login</a>
            <a class="nav-link custom-signup-btn1" href="/signup">Register </a>
          <% } else { %>
                   <% if (currUser.role === "student" ) { %>
                    <a class="nav-link custom-button1 me-5" href="/<%= currUser._id %>/trackapplication">Track Your Application</a>
                    <% } else if (currUser.role === "admin") { %>
                      <a class="nav-link custom-button1 me-5" href="/listings/<%= currUser._id %>/youritems">All Applications</a>
                    <% } else if (currUser.role === "college") { %>
                      <a class="nav-link custom-button1 me-5" href="/collegeverification">Scholarship College Verification</a>
                   <% } else if (currUser.role === "bank") { %>
                    <a class="nav-link custom-button1 me-5" href="/listings/<%= currUser._id %>/youritems">Scholarship Bank Verification</a>
                   <% } %> 
           
           </div>
           <div class="div">
           
            <a class="nav-link text-dark me-2" href="/listings/<%= currUser._id %>/profile">  <% if (currUser.image && currUser.image.url) { %>
              <img src="<%= currUser.image.url %>" alt="Profile Image" class="profile-img rounded-circle me-2">
            <% } else { %>
              <i class="fas fa-user-circle fa-3x me-2 profile-img rounded-circle me-2"></i>
            <% } %>
             <%= currUser.username %></a>
            </div>
            <a class="nav-link text-dark ms-3 me-2" href="/logout">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
  







  